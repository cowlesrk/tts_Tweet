<%= form_for(@twerk) do |f| %>

<% if @twerk.errors.any? %>
    <div class="error_explanation">
        <h2> <%= pluralize(@twerk.errors.count, "error") %> prohibited this twerk from being saved: </h2>
        
        <ul>
            <% @twerk.errors.full_messages.each do |message| %>
            <li> <%= message %> </li>
            <% end %>
        </ul>
    </div>
<% end %>

<div class="field">
    <%= f.label :message %><br>
    <%= f.text_field :message %>
</div>

<div class="field hidden">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id, value: current_user.id %>
</div>

<div class="actions">
    <%= f.submit "Twerk!", class: "btn btn-primary" %>
</div>

<% end %>